name: linux-gcc-x64-tsan
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
jobs:
  cloud-ci:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: install-tools
      run: |
        sudo apt-get update
        sudo apt-get install cmake ninja-build gcc-14 g++-14
    - name: build
      env:
        CC: gcc-14
        CXX: g++-14
        TYPE: Release
      run: |
        $CC --version
        $CXX --version
        mkdir build && cd build
        cmake -GNinja -DCMAKE_BUILD_TYPE=${TYPE} -DIHFT_SANITIZER_THREAD=1 ..
        cmake --build .
    - name: test
      run: cd build && ctest
